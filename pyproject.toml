[project]
name = "doh"
version = "0.1.0"
description = "Spark an argument. Realize you're wrong. Say the word."
requires-python = ">=3.12"
dependencies = [
    "pandas==2.3.3",
    "pandera==0.32.1",
    "numpy==2.5.0",
]
